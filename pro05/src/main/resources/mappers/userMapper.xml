<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chunjae.pro05.persis.UserMapper">

    <select id="findUserByLoginId" resultType="com.chunjae.pro05.entity.User">
        select userId, active, loginId, userName, password
        from user
        <where>
            <if test=" loginId != null and loginId != '' ">
                and loginId = #{loginId}
            </if>
        </where>
    </select>

    <insert id="setUserInfo" parameterType="com.chunjae.pro05.entity.User" useGeneratedKeys="true">
        insert into user (active, loginId, userName, password)
        values (#{param.active}, #{param.loginId}, #{param.userName}, #{param.password})
    </insert>

</mapper>